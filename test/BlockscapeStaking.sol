pragma solidity 0.8.16;

// SPDX-License-Identifier: BUSL-1.1

import "forge-std/Test.sol";

abstract contract BlockscapeStakingHelperContract is Test {
    // function _testSetMetadata() internal {
    //     uint256 tokenID = 1;
    //     uint256 stakedETH = 1 ether;
    //     uint256 stakedTimestamp = block.timestamp;
    //     assertEq(getMetadata(tokenID).stakedETH, 0 ether);
    //     assertEq(getMetadata(tokenID).stakedTimestamp, 0);
    //     _setMetadataForStakeInternal(stakedETH, tokenID);
    //     assertEq(getMetadata(tokenID).stakedETH, stakedETH);
    //     assertEq(getMetadata(tokenID).stakedTimestamp, stakedTimestamp);
    // }
}
